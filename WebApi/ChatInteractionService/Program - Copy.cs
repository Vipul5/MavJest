using MavJest.Service;
using Microsoft.SemanticKernel;
using Microsoft.SemanticKernel.ChatCompletion;
using OllamaSharp;
using OllamaSharp.Models.Chat;
using System.Runtime.CompilerServices;
using System.Text;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace MavJest.Service
{
    public class abc1
    {
    public void abc()
    {
            //var uri = new Uri("http://localhost:11434");
            //var ollama = new OllamaApiClient(uri);

            //// select a model which should be used for further operations
            //ollama.SelectedModel = "phi3:mini";

            //var models = await ollama.ListLocalModels();

            //await foreach (var status in ollama.PullModel("phi3:mini"))
            //    Console.WriteLine($"{status.Percent}% {status.Status}");

            //Console.WriteLine("Server Connected. Ollama is Running and loaded.");

            //// Create a web application builder
            //var builder = WebApplication.CreateBuilder(args);

            //// Register the GreetingService with the DI container
            //builder.Services.AddScoped<IChatService, ChatService>();


            //// Build the app
            //var app = builder.Build();

            //string fileName = "./data.json";
            //// Define a route that uses the ChatService
            //app.MapGet("/", (IChatService chatService) => chatService.convertFiletoJSON(fileName));

            //// Run the application
            //app.Run();
        }

    }
}